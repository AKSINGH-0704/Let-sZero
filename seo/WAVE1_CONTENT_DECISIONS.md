# Wave 1 Content Decisions

Wave 1 retains **290 new RepMail articles** from the 299-row selection after intent review. The nine removed files were untracked new additions and were removed because their search intent was materially duplicative of a retained canonical page.

| Removed slug | Canonical retained slug | Decision |
|---|---|---|
| `multiple-spf-records-safe-consolidation` | `multiple-spf-records-fix` | Merge the diagnosis and safe-consolidation workflow into one canonical page. |
| `spf-permerror-dns-lookup-limit` | `spf-too-many-dns-lookups` | Treat permerror as the lookup-limit symptom within one troubleshooting page. |
| `new-job-trigger-cold-email` | `event-triggered-follow-up-taxonomy` | Keep trigger qualification and examples in one broader trigger workflow. |
| `funding-trigger-cold-email` | `event-triggered-follow-up-taxonomy` | Avoid a second trigger-template page with the same evidence and stop-rule intent. |
| `deliverability-tool-migration-checklist` | `cold-email-tool-migration-checklist` | Keep migration guidance in the retained tool-migration workflow pending a distinct evidence-export scope. |
| `outreach-list-data-retention-deletion` | `cold-outreach-data-retention-deletion` | Keep retention/deletion as one compliance and operational task, not an academy split. |
| `dmarc-aggregate-report-triage` | `dmarc-aggregate-reports-rua` | Consolidate aggregate-report interpretation and triage. |
| `triage-dmarc-aggregate-reports` | `dmarc-aggregate-reports-rua` | Consolidate aggregate-report interpretation and triage. |
| `shared-vs-user-mailbox-outbound` | `email-alias-vs-separate-mailbox` | Keep outbound identity architecture under one explicit decision boundary. |

All retained pages use the existing Resource Center schema, author record, academy routes, collection/path graph, metadata behavior, structured-data implementation, search index, and related-content system. The decision log does not authorize redirects or deployment by itself; any previously exposed subordinate URL must be handled through the established route/deployment policy before publication.
